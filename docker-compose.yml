services:
  frontend:
    build: ./frontend
    ports:
      - '3010:3000'
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
    environment:
      - NODE_ENV=development

  backend:
    build: ./backend
    ports:
      - '5010:5000'
    volumes:
      - ./backend:/app
    command: npm run dev
    environment:
      - NODE_ENV=development
